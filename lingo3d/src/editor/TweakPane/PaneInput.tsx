export type PaneInputProps = {
    name: string
    value: any
    values?: Array<any>
    onChange?: (value: any) => void
}

const PaneInput = (props: PaneInputProps) => {
    return null
}

export default PaneInput
