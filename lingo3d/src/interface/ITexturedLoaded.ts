export default interface ITexturedLoaded {
    metalnessFactor: number
    roughnessFactor: number
}

export const texturedLoadedDefaults: ITexturedLoaded = {
    metalnessFactor: 1,
    roughnessFactor: 1
}