// import { ShaderChunk } from "three"
// import pcss, { getShadow } from "./pcss"

// let shader = ShaderChunk.shadowmap_pars_fragment
// shader = shader.replace("#ifdef USE_SHADOWMAP", "#ifdef USE_SHADOWMAP" + pcss)
// shader = shader.replace(
//     "#if defined( SHADOWMAP_TYPE_PCF )",
//     getShadow + "#if defined( SHADOWMAP_TYPE_PCF )"
// )
// ShaderChunk.shadowmap_pars_fragment = shader
