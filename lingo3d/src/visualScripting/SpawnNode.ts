import Appendable from "../api/core/Appendable"

export default class SpawnNode extends Appendable {
    public constructor() {
        super()
        
        this.createEffect(() => {

        }, [])
    }
}
