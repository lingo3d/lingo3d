import {
  foundManagerSchema,
  foundManagerDefaults
} from "lingo3d/lib/interface/IFoundManager"
import makeProps from "./utils/makeProps"

export default makeProps(foundManagerSchema, foundManagerDefaults)
