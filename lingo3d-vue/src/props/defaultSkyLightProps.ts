import {
  defaultSkyLightSchema,
  defaultSkyLightDefaults
} from "lingo3d/lib/interface/IDefaultSkyLight"
import makeProps from "./utils/makeProps"

export default makeProps(defaultSkyLightSchema, defaultSkyLightDefaults)
