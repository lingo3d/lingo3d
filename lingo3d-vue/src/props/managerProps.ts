import { objectManagerDefaults } from "lingo3d/lib/interface/IObjectManager"

const props = {
    x: {
        type: Number,
        default: objectManagerDefaults.x
    },
    y: {
        type: Number,
        default: objectManagerDefaults.y
    }
}

export default props