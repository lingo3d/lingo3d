import ObjectManager from "lingo3d/lib/display/core/ObjectManager"
import { ref, VNode } from "vue"

export const refreshOutline = ref({})

export const elements: Array<[Array<VNode>, ObjectManager]> = []